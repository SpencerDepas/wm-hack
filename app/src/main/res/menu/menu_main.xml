<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="clearfaun.com.wm_hackathon.MainActivity">
    <item android:id="@+id/action_settings"
          android:title="@string/search_wm_rating"
          android:orderInCategory="100"
          app:showAsAction="never"/>
    <item android:id="@+id/action_search_distance"
          android:title="@string/search_distance"
          android:orderInCategory="100"
          app:showAsAction="never"/>

    <item android:id="@+id/search_type"
          android:title="@string/search_type"
          android:orderInCategory="100"
          app:showAsAction="never"/>

</menu>
